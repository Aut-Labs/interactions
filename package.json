{
  "name": "interactions",
  "description": "Interactions smart contracts repository",
  "version": "1.0.0",
  "author": {
    "name": "",
    "url": "https://github.com/Aut-Labs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@openzeppelin/contracts-upgradeable": "^5.0.2"
  },
  "devDependencies": {
    "@prb/test": "^0.6.4",
    "forge-std": "github:foundry-rs/forge-std#v1.7.5",
    "prettier": "^3.0.0",
    "prettier-plugin-solidity": "^1.3.1",
    "sol2uml": "^2.5.20",
    "solhint": "^3.6.2"
  },
  "private": true,
  "scripts": {
    "clean": "rm -rf cache out",
    "build": "forge build",
    "test": "forge test",
    "test:coverage": "forge coverage",
    "test:coverage:report": "forge coverage --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage",
    "prettier:apply": "prettier --write '**/*.sol'",
    "prettier:check": "prettier --check '**/*.sol'"
  }
}
